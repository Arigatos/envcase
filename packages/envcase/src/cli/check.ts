// TODO: implement `envcase check` command

/**
 * Validates the current `.env` file against the schema without running the app.
 *
 * @example
 * ```bash
 * npx envcase check
 * # → ✅ All environment variables are valid.
 * ```
 */
export async function runCheck(): Promise<void> {
  // TODO: implement
  throw new Error('envcase check: not yet implemented')
}
