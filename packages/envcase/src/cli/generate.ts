// TODO: implement `envcase generate` command

/**
 * Reads the user's env schema and generates a `.env.example` file.
 *
 * @example
 * ```bash
 * npx envcase generate
 * # → writes .env.example
 * ```
 */
export async function runGenerate(): Promise<void> {
  // TODO: implement
  throw new Error('envcase generate: not yet implemented')
}
